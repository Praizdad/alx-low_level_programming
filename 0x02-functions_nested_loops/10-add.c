#include "main.h"
/**
 * add - add two integers and returns the result
 * @a: the first integer
 * @b: the second integer
 *
 * Return: (0)
 */
int add(int a, int b)
{
	int result = a + b;

	return (result);
}
